import React from "react";

const SectionCard = ({ title }) => (
  <div>
    <h2 className="text-2xl font-heading font-thin italic text-accent">
      {title}
    </h2>
  </div>
);

export default SectionCard;
